namespace 灵犀笔试;

public class PerfectTravel
{
    public static void Main2()
    {
        while (true)
        {
            // n个不同地点 m条不同线路
            // 先保证最大愉悦值 再求总时间
            
            var nm = Console.ReadLine().Split();
            int n = int.Parse(nm[0]);
            int m = int.Parse(nm[1]);
            if(n == 0 && m == 0) return;
            
            for (int i = 0; i < m; i++)
            {
            
            }

        }
    }

}